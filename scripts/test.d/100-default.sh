#!/bin/bash
# Executes application tests.
#
# TODO(jordoncm): This is just a blank stub at this point. Need select default
# testing frameworks for the scaffolding and do a reference implementation.

cd `dirname $0`
